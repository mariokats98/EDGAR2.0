{
  "name": "herevna",
  "version": "0.4.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "postinstall": "sh -c 'export DATABASE_URL=\"${DATABASE_DATABASE_URL:-$DATABASE_URL}\" && export DATABASE_DATABASE_URL=\"${DATABASE_DATABASE_URL:-$DATABASE_URL}\" && prisma generate'",
    "build:tickers": "node -e \"console.log('Skipping build:tickers (not used)')\"",
    "build": "sh -c 'export DATABASE_URL=\"${DATABASE_DATABASE_URL:-$DATABASE_URL}\" && export DATABASE_DATABASE_URL=\"${DATABASE_DATABASE_URL:-$DATABASE_URL}\" && prisma migrate deploy && prisma generate && next build'",
    "start": "next start",
    "lint": "next lint",
    "prisma:studio": "prisma studio"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@next-auth/prisma-adapter": "1.0.7",
    "@prisma/client": "5.22.0",
    "bcryptjs": "2.4.3",
    "clsx": "2.1.1",
    "next": "14.2.5",
    "next-auth": "4.24.11",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "stripe": "^14.24.0",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@types/bcryptjs": "2.4.6",
    "@types/node": "20.14.9",
    "@types/react": "18.3.3",
    "@types/react-dom": "18.3.0",
    "autoprefixer": "10.4.20",
    "eslint": "8.57.0",
    "eslint-config-next": "14.2.5",
    "postcss": "8.4.41",
    "prisma": "5.22.0",
    "tailwindcss": "3.4.10",
    "typescript": "5.5.4"
  }
}